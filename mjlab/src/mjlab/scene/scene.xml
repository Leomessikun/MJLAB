<!-- Base scene for mjlab: visuals, ground plane, and optional assets. -->
<mujoco model="mjlab scene">
  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0"/>
    <rgba force="1 0 0 1" haze="0.15 0.25 0.35 1"/>
    <global azimuth="135" elevation="-25" offwidth="1920" offheight="1080"/>
    <map force="0.005"/>
    <scale forcewidth="0.25" contactwidth="0.4" contactheight="0.15" framelength="5.0" framewidth="0.3"/>
    <quality shadowsize="8192"/>
  </visual>
  <statistic meansize="0.03"/>

  <!-- Simple textured ground plane -->
  <asset>
    <!-- Use unique names to avoid collisions when terrain importer also defines a ground texture -->
    <texture type="2d" name="mjlab_scene_groundplane" builtin="checker" rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3" width="512" height="512"/>
    <material name="mjlab_scene_ground_mat" texture="mjlab_scene_groundplane" texrepeat="10 10" reflectance="0.2"/>
  </asset>

  <worldbody>
    <light name="top_light" pos="0 0 2.0" dir="0 0 -1" diffuse="0.8 0.8 0.8" specular="0.1 0.1 0.1" castshadow="true"/>

    <geom name="ground" type="plane" size="10 10 0.1" material="mjlab_scene_ground_mat" contype="1" conaffinity="1"/>

    <!-- Object is attached via Scene entities (see G1FlatEnvCfg) -->
  </worldbody>
</mujoco>
